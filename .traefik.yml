displayName: API Manager Plugin
type: middleware
iconPath: .assets/icon.png

import: github.com/MGDIS/traefik-apimanager-plugin

summary: 'Traefik plugin to automatically call API Manager to replace requests headers'

testData:
  authMode: 'oauth2'
  apiManagerURL: 'https://api-manager-url'
  clientID: 'client_id'
  username: 'user'
  password: 'pass'
  grantType: 'password'